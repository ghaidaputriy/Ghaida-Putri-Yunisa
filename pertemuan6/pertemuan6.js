let nama = "Ghaida"; // Menggunakan let karena nilai akan diubah
nama = "putri"; // Mengubah nilai variabel nama
console.log(nama); // Menampilkan nilai nama yang baru

// tipe data primitif
// number, string, boolean, null, undefined, symbol, bigint

let i = 123; // Menggunakan nama variabel yang valid
console.log(i); // Menampilkan nilai variabel i
